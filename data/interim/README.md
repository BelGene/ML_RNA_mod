# Interim Data

This directory contains normalized source-specific parquet files generated by
the Snakemake ingest stages. These files are ignored by Git and can be rebuilt
from raw downloads plus tracked manual tables.
