# Data Layout

Tracked:

- `manual/`: small manually curated source tables.
- `raw/README.md`, `interim/README.md`, `processed/README.md`: directory policy notes.

Ignored and regenerated:

- `raw/`: public source downloads.
- `interim/`: normalized source-specific parquet files.
- `processed/`: final model-ready dataset artifacts.
